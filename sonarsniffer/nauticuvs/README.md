# nauticuvs-full (vendored)

Bundled inside the SonarSniffer repository for offline / CI builds.

- **Not published** to crates.io (`publish = false` in `Cargo.toml`)
- **Path dependency only** — root CLI: `./nauticuvs`, desktop Tauri: `../../nauticuvs`
- Do not replace with a registry version; curvelet FDCT must ship with this repo
