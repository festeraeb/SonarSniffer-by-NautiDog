# cesarops-node config — example for cesarops2 (P1000 intake brain)
# Copy to each node and adjust gpu_id, backend, models_dir, koboldcpp_path.

forge_url = "http://10.0.0.1:9100"
node_name = "cesarops2"
models_dir = "/mnt/storage/models"
listen_port = 5500
gpu_id = 1
backend = "cuda"
koboldcpp_path = "/home/cesarops/benchmark/koboldcpp"
heartbeat_interval_secs = 10
max_restarts = 3
