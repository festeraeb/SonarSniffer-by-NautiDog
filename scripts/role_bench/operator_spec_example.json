{
  "spec_version": 1,
  "title": "Example — wire spec-draft bench into Forge",
  "given": {
    "context": "cesarops-forge-v2 on c2 :9100; local LLMs on :5200 (RTX) and :5203 (1070 ZAYA).",
    "constraints": [
      "No P100 endpoints during satellite runs",
      "Human must approve spec before /send executes tools"
    ],
    "paths": {
      "repo": "/data/codebase/repos/wreckhunter2000-1",
      "forge_config": "cesarops-forge-v2/cluster_config.toml"
    }
  },
  "do": [
    {
      "step": 1,
      "action": "Add POST /spec/draft route that calls configured local endpoint",
      "success": "curl returns OperatorSpec JSON"
    }
  ],
  "deliver": {
    "artifacts": ["scripts/role_bench/run_spec_draft_bench.py"],
    "acceptance": [
      "Bench scores JSON validity on :5200 and :5203 only",
      "Results written under var/role_bench/spec_draft/"
    ]
  },
  "risks": ["Model outputs invalid JSON — grader catches and scores low"],
  "needs_user_approval": true
}
