[
  {
    "id": "03_phase_correlation",
    "route": "1",
    "prompt_profile": "tight",
    "target_package": "cesarops-satellite",
    "target_files": ["src/phase_corr.rs", "src/temporal.rs"],
    "task": "Implement 2D phase correlation coregistration for run_temporal_stack_local: new src/phase_corr.rs (rustfft, downsample max 512, epsilon 1e-12, 3x3 parabolic sub-pixel), warp clarity planes to reference before LOO persistence. Calibrate Burns 45.87127,-84.58642 and Cedarville 45.7873,-84.6708. Success: temporal persistence peak >= 0.30.",
    "experiment_metadata": {
      "log_target": "implementor_learnings.jsonl",
      "experiment_key": "phase_correlation_fft",
      "success_metrics": "temporal_stack persistence peak >= 0.30 over Burns/Cedarville anchors"
    },
    "pipeline_prompts": {
      "step_1_thinker_mixtral": {
        "role": "Master Architect / Signal Processing Analyst",
        "prompt": "CONTEXT: We are resolving the 0.08 temporal persistence bug within the `cesarops-satellite` package. Small sub-pixel orbital drift in Sentinel-2 multi-date scenes (2022-2024) is blurring out the persistence signatures of the Burns (wood, 34m) and Cedarville (steel, 32m) wreck calibration targets in the local temporal stack (`src/temporal.rs`). TASK: Design a rigorous step-by-step mathematical pseudo-specification for a 2D Phase Correlation routine inside a new module `src/phase_corr.rs`. CONSTRAINTS: 1. Coregistration must run on a downsampled variation of the clarity bands (max dimension bounded to 512x512) or a wreck-centered chip array to preserve CPU cycles on our T440 Node 0 layout, then scale calculated translations back to the original resolution. 2. Define the cross-power spectrum matrix computation: R = (F_ref * Conj(F_tgt)) / (|F_ref * Conj(F_tgt)| + 1e-12) to isolate translational shift parameters. 3. Specify a local 3x3 parabolic interpolation around the maximum peak in the spatial domain to extract fine sub-pixel translations (dx, dy). 4. Detail how `run_temporal_stack_local` must warp each sequential clarity plane to a fixed historical reference scene prior to computing the final Leave-One-Out (LOO) persistence map. DO NOT output complete Rust code. Provide only the architectural spec and matrix layout paths."
      },
      "step_2_coder_gemma": {
        "role": "Low-Level Algorithmic Developer",
        "prompt": "TASK: Implement the Phase Correlation specification generated by the Thinker inside `cesarops-satellite/src/phase_corr.rs` and add the execution hook into `src/temporal.rs`. MANDATED SYSTEM CONSTRAINTS: 1. Output code blocks only. Do not generate introductions or conversational prose. 2. DO NOT hand-roll or initialize a raw radix-2 FFT algorithm. Utilize your existing `nauticuvs` module or native `rustfft` calls (`fft2_inplace` / `ifft2_inplace`) for all frequency domain transformations. 3. All vector processing loops must use flat, row-major contiguous `&[f64]` slices to align with the unified T440 memory configuration. 4. Function signature must return a strictly typed `Result<(f64, f64), EngineError>` to maintain alignment with Lane B ecosystem boundaries. 5. Explicitly output a section titled '## Files touched' outlining the exact call-site changes inside `run_temporal_stack_local` to map the incoming data matrix array prior to the persistence calculation loop."
      },
      "step_3_polisher_mixtral": {
        "role": "Senior Quality Assurance & Performance Auditor",
        "prompt": "TASK: You are the Polisher. Review the generated Rust code for `src/phase_corr.rs` and the structural hook inside `src/temporal.rs` written by the Coder. VALIDATION METRICS: 1. Ensure the denominator calculation in the cross-power matrix multiplication block features an explicit epsilon guard (`+ 1e-12`) to block division-by-zero panics. 2. Confirm all matrix float arrays are strictly explicitly bound to primitive `f64` types. 3. Audit the parabolic peak interpolation block to verify it uses a strict 3x3 pixel bounds offset validation array. 4. Check that downsampled shifts are correctly scaled back to full-resolution spatial indices without rounding biases. Output only the hardened, production-ready Rust file code blocks."
      }
    }
  }
]
