{
    "top_p":  0.9,
    "max_length":  8192,
    "prompt":  "You are implementing the CesarOps warp-grid crate Phase 1. A senior researcher reviewed your first attempt and found critical issues. Write the CORRECTED implementation from scratch.\r\n\r\nCORRECTIONS TO APPLY:\r\n\r\n1. REMOVE hwloc2 â€” API is insufficient. Use /sys filesystem instead:\r\n   - Parse /sys/bus/pci/devices/\u003cBUS_ID\u003e/numa_node for GPU-to-socket mapping\r\n   - Use libc::sched_setaffinity for thread pinning\r\n   - Count /sys/devices/system/node/nodeN directories for dynamic NUMA node detection (handles SNC)\r\n\r\n2. REMOVE nvml-wrapper â€” incompatible with NVIDIA driver 580.x (Pascal legacy).\r\n   - Parse nvidia-smi --query-gpu=utilization.gpu,memory.used,memory.total,temperature.gpu,power.draw --format=csv,noheader,nounits\r\n   - Use tokio::process::Command for async execution\r\n\r\n3. SERIALIZATION: Arc\u003cAtomicU64\u003e needs #[serde(skip)]. Features must be Vec\u003cString\u003e not Vec\u003c\u0026\u0027static str\u003e.\r\n\r\n4. WGPU F16: Check adapter.features().contains(wgpu::Features::SHADER_F16) at init. Store flag in DeviceProfile.\r\n\r\n5. REGISTER PRESSURE: Count var/let declarations in WGSL source. Warn if \u003e20, reject if \u003e32 for P100.\r\n\r\n6. ADD thiserror and libc to deps. REMOVE hwloc2 and nvml-wrapper.\r\n\r\nOUTPUT ALL 5 FILES:\r\n\r\nFILE 1: warp-grid/Cargo.toml\r\nDependencies: wgpu 29, naga 29, quinn 0.11, axum 0.8, tokio 1 (full), serde 1, serde_json 1, bincode 1, ndarray 0.16, half 2, bytemuck 1 (derive), rayon 1.10, wasmtime 25, mdns-sd 0.11, self-replace 1, tracing 0.1, tracing-subscriber 0.3, uuid 1 (v4), thiserror 1, libc 0.2. Edition 2024.\r\n\r\nFILE 2: warp-grid/src/types.rs\r\n- Precision enum (FP16/FP32/FP64/INT8)\r\n- NvidiaArch enum (Pascal/Volta/Turing/Unknown with String payload)\r\n- Hardware enum (Pascal/Turing/Volta/Xeon/TPU/RemoteNode/WebGpu)\r\n- BanLevel enum (KLine/GLine/ZLine)\r\n- DispatchTier enum (Specialist/Adaptive/Preprocessor)\r\n- ComputeTask struct (data_size, precision, shader_path, params)\r\n- Tensor struct (data: Vec\u003cu8\u003e, shape: Vec\u003cusize\u003e, dtype: Precision)\r\n- DeviceProfile struct (name, arch, sm_version, vram_total_mb, vram_free_mb, tflops, bandwidth_gbps, utilization: Arc\u003cAtomicU64\u003e with #[serde(skip)], socket_id: Option\u003cu32\u003e, has_f16: bool)\r\n- GpuNode struct (name, arch, sm_version, features: Vec\u003cString\u003e, vram_mb, bandwidth_gbps)\r\n- Error enum via thiserror (DeviceNotFound, ShaderCompileFailed, QuicTimeout, KLined, VramExhausted, NumaMismatch, IoError, ParseError)\r\n\r\nFILE 3: warp-grid/src/numa.rs\r\n- NumaTopology struct (socket_count, gpu_affinity: Vec\u003c(u32, u32)\u003e, cores_per_socket: Vec\u003cVec\u003cu32\u003e\u003e)\r\n- NumaTopology::detect() â€” read /sys/devices/system/node/ to count nodes, read /sys/bus/pci/devices/*/numa_node for GPU mapping\r\n- pin_worker_to_socket(socket_id, cores) â€” use libc::sched_setaffinity with CPU_SET macro\r\n- GPU discovery: enumerate /sys/bus/pci/devices/, filter for NVIDIA vendor (0x10de), read numa_node file\r\n- Handle SNC: if node_count \u003e 2, log info about Sub-NUMA Clustering detected\r\n\r\nFILE 4: warp-grid/src/lib.rs\r\n- mod types, numa, pool, metrics\r\n- pub use types::*\r\n- WarpGrid struct with numa: NumaTopology\r\n- WarpGrid::new() async â€” calls NumaTopology::detect()\r\n- use tracing::info\r\n\r\nFILE 5: warp-grid/src/pool.rs (stub)\r\n- pub struct VirtualComputePool\r\n\r\nFILE 6: warp-grid/src/metrics.rs\r\n- GpuMetric struct (gpu_index, utilization_pct, memory_used_mb, memory_total_mb, temperature_c, power_draw_w)\r\n- pub async fn query_gpu_metrics() -\u003e Result\u003cVec\u003cGpuMetric\u003e, Error\u003e â€” runs nvidia-smi, parses CSV output\r\n- pub fn estimate_register_pressure(wgsl_source: \u0026str) -\u003e u32 â€” counts var/let lines\r\n\r\nSTYLE: enum dispatch (no trait objects), tracing for logging, Result\u003cT, Error\u003e everywhere, borrow-before-move, explicit numeric types.",
    "temperature":  0.2
}