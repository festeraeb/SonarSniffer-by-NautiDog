# Staged fixes (Cake idle audit)

JSON audits from `scripts/cesarops-cake-audit-loop.sh` land here. **Never auto-applied** — review and merge manually.

Metrics append to `~/.cache/cesarops/audit_runs.jsonl` for optional nautivecs ingest (`scripts/p106_metrics_ingest.sh`).
